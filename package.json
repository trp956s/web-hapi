{
  "name": "web-hapi",
  "version": "1.0.0",
  "description": "example hapi.js REST server for addresses",
  "main": "index.js",
  "scripts": {
    "test": "concurrently \"npm start\" \"node -e 'setTimeout(function() {}, 2000);' && jasmine-node --verbose . && jasmine\" --kill-others --success all",
    "start": "node api/server.js",
    "boot-up-database": "concurrently \"mkdir -p data && mongod --dbpath=./data --port 27017\" \"node -e 'setTimeout(()=>console.log(\\\"database running on mongodb://localhost:27017\\\"), 2000)'\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/trp956s/web-hapi.git"
  },
  "author": "tom pepe",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/trp956s/web-hapi/issues"
  },
  "homepage": "https://github.com/trp956s/web-hapi#readme",
  "dependencies": {
    "hapi": "^16.5.2",
    "hapi-swagger": "^7.7.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0",
    "frisby": "^2.0.2",
    "jasmine": "^2.7.0",
    "jasmine-node": "^1.14.5",
    "mongodb": "^2.2.31"
  }
}
